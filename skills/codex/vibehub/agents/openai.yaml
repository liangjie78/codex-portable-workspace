interface:
  display_name: "VibeHub"
  short_description: "按需识别准确术语、改写口语需求并提供内链"
  default_prompt: "使用 $vibehub 识别这段需求中影响理解的准确术语，给出通俗解释，并在有可靠匹配时提供 VibeHub 内链。不要扩展成课程或通用术语清单。"

policy:
  allow_implicit_invocation: true
