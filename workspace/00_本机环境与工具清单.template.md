# 00_本机环境与工具清单

本文件由每台机器根据实际检查结果维护，不从旧机器直接照搬。不要记录密码、token、Cookie、完整认证文件、生产地址或一次性敏感命令。

## 核心路径

```text
Workspace: {{WORKSPACE_ROOT}}
Codex home: {{CODEX_HOME}}
```

## 运行环境

| 项目 | 实际结果 | 备注 |
| --- | --- | --- |
| WSL distribution | 待检测 | 应为 Ubuntu-24.04 |
| WSL version | 待检测 | 应为 2 |
| Shell | 待检测 | 应为 `/usr/bin/bash` |
| Codex agent | 待检测 | 应运行在 WSL，而不是 `docker-desktop` |
| WSL networking | 待检测 | 记录 mirrored、autoProxy、DNS tunneling 状态 |

## 基础工具

```bash
command -v bash git curl python3 node npm codex bwrap
bash --version | head -n 1
git --version
python3 --version
node --version
npm --version
codex --version
```

## 本机服务

只记录服务名称、用途和端口；账号、密码和连接密钥只放在本机安全配置中。

| 服务 | 端口/用途 | 状态 |
| --- | --- | --- |
| 待填写 | 待填写 | 待检测 |

## Codex 连接与网络

- 模型、插件、MCP 和权限以本机 Codex 设置及实际启动结果为准。
- 认证、会话、缓存、任务数据和本机知识库不进入工作区或仓库。
- VPN、DNS 或代理发生变化时，先记录脱敏现象，再运行 `bash scripts/doctor.sh`。
